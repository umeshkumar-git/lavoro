{
  "name": "lavoro",
  "version": "1.0.0",
  "private": true,
  "description": "Lavoro daily assistant app.",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend start",
    "health": "npm --prefix backend run health"
  }
}
